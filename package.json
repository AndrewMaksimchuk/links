{
  "name": "links",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "hono": "^4.4.6",
    "react": "^18.3.1"
  },
  "scripts": {
    "install": "bun install",
    "run": "bun run server.index.tsx",
    "dev": "BUN_CONFIG_VERBOSE_FETCH=curl bun --watch --inspect run server.index.tsx",
    "test": "bun test --watch",
    "check": "bunx tsc --strict --noEmit --watch"
  }
}
